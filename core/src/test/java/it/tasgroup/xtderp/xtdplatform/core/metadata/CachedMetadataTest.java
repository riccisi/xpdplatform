package it.tasgroup.xtderp.xtdplatform.core.metadata;

import org.junit.Test;

public class CachedMetadataTest {

    @Test
    public void test() {

/*
        assertThat("subsequent calls are different", metadata.iterator().next(), not(equalTo(metadata.iterator().next())));

        CachedMetadata cachedModels = new CachedMetadata(metadata);

        assertThat("subsequent calls are equals", cachedModels.iterator().next(), equalTo(cachedModels.iterator().next()));
*/

    }
}