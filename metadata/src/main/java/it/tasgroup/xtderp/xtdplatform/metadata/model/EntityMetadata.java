package it.tasgroup.xtderp.xtdplatform.metadata.model;

import java.io.Serializable;

public interface EntityMetadata<T> extends ModelMetadata {

/*    Serializable idOf(T entity);

    void save(T entity);

    void delete(T entity);*/

}